import 'package:flutter/material.dart';

class CourseStudentScreen extends StatelessWidget {
  static const routeName = '/course_student_screen';

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}